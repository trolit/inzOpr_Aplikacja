@model IEnumerable<crud.Models.Imprezy>

@{
    <!-- tytul strony -->
    ViewBag.Title = "Lista imprez";
}

<head>
    <!-- zalaczenie biblioteki globalnej style.css -->
    <link rel="Stylesheet" href="~/Content/style.css" type="text/css" />
</head>

<div class="naglowekStrony">
    Lista dostępnych imprez
</div>

<table class="table">
    <tr class="text-center">
        <th>
            Nazwa imprezy
        </th>
        <th>
            Rodzaj/treść
        </th>
        <th>
            Miejsce
        </th>
        <th>
            Liczba uczestników
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr class="parzysty nieparzysty text-center">
            <td>
                @Html.DisplayFor(modelItem => item.nazwaImprezy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.rodzaj)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.miejsce)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.liczbaUczestnikow)
            </td>
            <td>
                @Html.ActionLink("Edytuj", "Imprezy_Edit", new { id = item.idImpreza }, new { @class = "btn przyciskSterowania3" }) 
                @Html.ActionLink("Szczegóły", "Przelozony_Imprezy_Details", new { id = item.idImpreza }, new { @class = "btn przyciskSterowania2" }) 
                @Html.ActionLink("Uczestnicy", "Uczestnicy_Lista", new { id = item.idImpreza }, new { @class = "btn przyciskSterowania2" }) 
                @Html.ActionLink("Usuń", "Imprezy_Delete", new { id = item.idImpreza }, new { @class = "btn przyciskSterowania4" })
            </td>
        </tr>
    }

</table>
@Html.ActionLink("Wróć", "Przelozony", null, null, new { @class = "btn przyciskPowrot" })